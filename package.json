{
  "name": "quizly",
  "version": "1.0.0",
  "description": "quiz app",
  "license": "ISC",
  "author": "",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run build --workspace=shared && concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && npm run build --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
